// Legacy frontend script retained for reference. The new React app lives in /frontend/src.
// This file is kept only for history; the React app (Vite) mounts into /src/main.jsx
// and handles the UI.

console.info(
  "Legacy frontend script retained — use the React app in /frontend/src instead."
);
